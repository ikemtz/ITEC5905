apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "itec5905.fullname" . }}-test-connection"
  labels:
    {{- include "itec5905.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "itec5905.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
