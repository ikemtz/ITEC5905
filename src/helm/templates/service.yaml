apiVersion: v1
kind: Service
metadata:
  name: {{ include "itec5905.fullname" . }}
  labels:
    {{- include "itec5905.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "itec5905.selectorLabels" . | nindent 4 }}
