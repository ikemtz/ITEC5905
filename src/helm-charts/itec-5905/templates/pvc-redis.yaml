apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-volume-claim
spec:
  storageClassName: {{ $.Values.environment.storageClass }}
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
---